import styled from 'styled-components'

const Message = styled.div`
    color: white;
`

export default function FilePanel(){
    return(
        <Message>
            There is an algorithm required here that needs closer inspection of GraphQL return values
        </Message>

    )
}